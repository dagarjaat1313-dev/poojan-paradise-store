import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{supabase}from'./supabase';import'./admin.css';
function Admin(){const[user,setUser]=useState(null),[role,setRole]=useState(null),[status,setStatus]=useState('Checking access…');useEffect(()=>{let mounted=true;(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){location.href='/login.html?next=/admin.html';return}const{data,error}=await supabase.from('user_roles').select('role').eq('user_id',user.id).maybeSingle();if(!mounted)return;if(error||data?.role!=='admin'){setStatus('Access denied — admin account required.');await supabase.auth.signOut();return}setUser(user);setRole('admin');setStatus('Admin access verified.')})();return()=>{mounted=false}},[]);const logout=async()=>{await supabase.auth.signOut();location.href='/login.html'};if(!user||role!=='admin')return <main className="admin-gate"><div className="admin-card"><div className="admin-logo">ॐ</div><h1>Poojan Paradise</h1><p>{status}</p>{status.startsWith('Access')&&<a href="/login.html">Back to Login</a>}</div></main>;return <div className="admin-shell"><aside><div className="admin-brand">ॐ <span>POOJAN<br/><b>PARADISE</b></span></div><nav><a className="active">Dashboard</a><a>Products</a><a>Orders</a><a>Customers</a><a>Loyalty Program</a><a>Settings</a></nav><button onClick={logout}>Sign out</button></aside><section className="admin-main"><header><div><span>ADMIN PANEL</span><h1>Dashboard</h1></div><div className="admin-user">{user.email}</div></header><div className="stats"><div><small>Products</small><strong>25</strong></div><div><small>Orders</small><strong>0</strong></div><div><small>Customers</small><strong>0</strong></div><div><small>Rewards</small><strong>Shagun & Para</strong></div></div><div className="welcome"><span>POOJAN PARADISE</span><h2>Admin dashboard is ready.</h2><p>Secure authentication and role protection are active. Product, order, customer and loyalty management can now be connected here.</p></div></section></div>}
createRoot(document.getElementById('admin-root')).render(<Admin/>);